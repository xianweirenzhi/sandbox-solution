# sandbox-solution 整体架构与预期完整拓扑

> 本文档由 Claude Code 维护，随仓库变更同步更新。最后更新：2026-09-09

## 1. 最终形态

主从式沙盘控制系统：**1 台 ESP32-S3 主机**作为控制台与通信枢纽，以「UDP 广播发现 + 6 路 TCP 数据端口」接入**至多 6 台 ESP8266 从机**（`F8266-x`，一台从机对应沙盘一个子系统，首台为智慧农场）；用户用任意浏览器访问主机网页即可监控全网状态、向任意从机下发命令。主机固件有先后两版：**现行部署为 [esp32-2](esp32-2/)（网页 v2 仪表盘 + 每端口独立 auto_ctrl）**，[esp32-1](esp32-1/) 为其前身、**保留作旧版备份**（网页 v1 + auto_ctrl 单实例，能力一致）。[esp8266-1](esp8266-1/) 为架构外独立演示节点，不参与主从通信。

通信骨架（发现面 + 数据面 + 双向自愈）与首个从机的业务闭环（传感 + 执行 + 命令词表 + 阈值自动控制）已全部实现并通过主从联调；第二台从机 F8266-2 已完成差异化改造并**上板联调通过**（无 MQ-2、GY-39 直连二合一、土壤改 AO 模拟量）；剩余工作集中在**从机扩展**（F8266-3~6 待建）与各从机现场标定，详见 §9。

## 2. 预期完整拓扑图

![预期完整拓扑图](docs/topology.svg)

> 图片版源文件 [docs/topology.svg](docs/topology.svg)（另附位图版 `docs/topology.png`，供不支持 SVG 的场合），随系统演进同步更新，自动适配浅色 / 深色阅读主题。

## 3. 层级职责

| 层级      | 节点                         | 职责                                                             | 现状                      |
| --------- | ---------------------------- | ---------------------------------------------------------------- | ------------------------- |
| ① 用户层 | 手机 / PC 浏览器             | 访问主机控制台，查看全网状态、向从机下发命令                     | ✅                        |
| ② 主机   | esp32-2（ESP32-S3，**现行**）· esp32-1（备份） | 控制台网页、UDP 广播宣告、6 路 TCP 从机端口、上下线/掉电管理、阈值自动控制（auto_ctrl；esp32-2 为每端口独立实例） | ✅                        |
| ③ 从机层 | F8266-1 ~ F8266-6（ESP8266） | 一台对应沙盘一个子系统：联网 + 主机链路 + 业务（传感/执行/显示） | ✅ F8266-1 已上线（传感①~⑤ + 执行实体 + 主机闭环控制）；✅ F8266-2 已上线（差异化：GY-39 直连二合一 + 土壤 AO + SGP30 + 执行实体，2026-09-09 联调通过） |
| ④ 独立岛 | esp8266-1（ESP8266）         | SoftAP 强制门户演示（ESP8266 介绍页），与主从架构无关            | ✅（定位待定）            |

## 4. 通信设计

### 4.1 发现面（UDP :45555）

- 主机 **STA 在线时**每 3s 向局域网广播 `ESP32HOST,<ip>,8000,6`（无 `@` 帧头）；
- 从机监听同一端口，收到有效宣告即把目标主机切到该 IP（若正连旧地址则断开重连）；
- 超过 20s（`NET_HOST_DISCOVER_STALE_MS`）未收到宣告，从机回退 `net_config.h` 硬编码地址 `192.168.1.100`；
- 效果：**主机可保持 DHCP 动态 IP**，从机零配置跟随主机 IP 变化。

### 4.2 数据面（TCP :8000 ~ :8005）

- 从机是 **TCP 客户端**，联网后主动连主机对应端口（`F8266-x` → `8000+(x-1)`）；每端口允许 ≤3 个连接（`PORT_CLIENTS_MAX`）；
- 帧协议：**`@` 为帧头、`/` 为帧尾，中间为命令有效内容**；单帧内容 ≤128 字符（从机 `NET_FRAME_MAX_LEN`，主机端解析上限 256），且不得含 `@` / `/`；
- 上线：从机连上即发 `@F8266-x online/`，主机网页对应端口卡标记「已上线」（绿点）；断开即标记离线；
- **数据上报**：从机每 2s（`NET_REPORT_MS`）发 JSON 帧 `@{"t":25.3,"h":56.0,"lux":1234,"soil":1,"co2":800,"tvoc":12,"pump":0,"fan":1,"servo":0}/`，主机端以 `{` 开头识别并解析存储（ArduinoJson），`null` 字段表示该传感器无数据；当前 F8266-1 上报温湿度/光照/土壤/eCO₂/TVOC + **执行器实时状态** `pump`(0/1)/`fan`(0停/1正/2反)/`servo`(0~180°，业务语义 **0°=大棚关/180°=大棚开**)（**MQ-2 仅本机使用，不上报**），主机据此在网页执行器卡显示当前态；F8266-2 同格式并**新增 `press`（hPa，GY-39 气压）**，主机端暂不解析（未知字段忽略，无冲突）；
- 下发：主机网页 `/api/send` 提交的文本自动包装为 `@内容/` 帧发出（执行器控制命令见 §8.2）；
- 掉电感知：从机异常掉电不发 FIN，主机端 **TCP keepalive**（空闲 3s 后每 2s 探测、3 次无响应）≈5~10s 内判定离线，无需从机固件配合；
- 健壮性：缺帧尾 `/` 的不完整帧，静默超过 200ms（`PORT_RX_TIMEOUT_MS`）按不完整帧强制结束处理。

### 4.3 控制面（HTTP :80）

| 接口            | 方法 | 说明                                                   |
| --------------- | ---- | ------------------------------------------------------ |
| `/`           | GET  | 主机控制台页面（状态卡 + 传感数据卡 + 执行器控制 + 自动控制 + 二级日志页） |
| `/api/status` | GET  | 主机设备状态 JSON                                      |
| `/api/ports`  | GET  | 6 个端口状态 JSON（在线从机、收发计数、最新传感+执行器状态、日志） |
| `/api/send`   | POST | 向指定端口下发命令（参数`port`、`text`，自动包帧；网页执行器按键经此发送） |
| `/api/auto`   | GET/POST | 读写阈值自动控制配置（总开关 + 高温/高CO₂/低湿阈值）与 4 条规则触发状态 |

## 5. 端口与身份分配

| 端口 / 地址                     | 用途                                        | 占用者                                    | 状态    |
| ------------------------------- | ------------------------------------------- | ----------------------------------------- | ------- |
| UDP :45555                      | 发现面广播（主机 → 全网）                  | esp32-2（现行）/ esp32-1（备份）          | ✅      |
| TCP :8000                       | 数据面：从机 1                              | F8266-1（智慧农场：5 传感子系统 + 执行实体已接入） | ✅      |
| TCP :8001                       | 数据面：从机 2                              | F8266-2（差异化气象节点：GY-39 直连二合一 + 土壤 AO→A0 + SGP30 + 执行器，上报 soil 0/1 不变、新增 press） | ✅ 联调通过 |
| TCP :8002 ~ :8005               | 数据面：从机 3~6                            | F8266-3 ~ F8266-6（各对应一个沙盘子系统） | ⬜ 预留 |
| TCP :80 / mDNS `esp32s3-2.local` | 控制面：用户浏览器（esp32-2 现行；esp32-1 备份为 `esp32s3.local`） | esp32-2（现行）/ esp32-1（备份） | ✅      |
| （回退）`192.168.1.100`       | 从机侧硬编码主机地址，仅宣告缺席 20s 后兜底 | esp32-2（现行）/ esp32-1（备份）         | ✅      |

> 命名体系：从机身份 = STA 主机名 = 配置热点名 = 项目目录名（如 `F8266-2`），全网唯一。

## 6. 自愈与兜底机制

| 场景                     | 机制                                                                                                | 关键参数                                       |
| ------------------------ | --------------------------------------------------------------------------------------------------- | ---------------------------------------------- |
| 主机连不上路由器         | 120s 超时切 AP 热点（esp32-2 现行：`ESP32-Direct-2`；esp32-1 备份：`ESP32-Direct`；均 192.168.4.1），手机直连仍可访问控制台；AP 为终态，重启才回 STA | `STA_TIMEOUT_MS`                             |
| 从机默认网络不可达       | 自动开同名配置热点`F8266-x`（WiFiManager，192.168.4.1）改配；180s 无人配置自动重启重试            | `NET_PORTAL_TIMEOUT_S`                       |
| 从机运行期断网           | 失联超 15s（SDK 自动重连未恢复）自动重连默认网络 / 重开配置热点；期间主机链路暂停，恢复后重连       | wifi_net 自愈                                  |
| 主机 IP 变化 / 重启换 IP | 从机收 UDP 宣告自动切换目标地址（旧连接断开重连）                                                   | 宣告周期 3s                                    |
| 主机离线                 | 从机按 3s 间隔重试 TCP 连接，重连成功重新上报上线                                                   | `NET_LINK_RETRY_MS`                          |
| 主机断开 / 从机断网       | 从机检测到连接由在线→断开即 `allStop()` 全停执行器（泵关/扇停/舵机 0°关棚——业务语义 0°=关棚/180°=开棚）；重连成功立即补发一次设备状态，随后不自动恢复（需主机重发命令） | `link.isOnline()` 状态过渡 |
| 从机异常掉电             | 主机 TCP keepalive ≈5~10s 判定离线，端口卡转灰                                                     | `PORT_KEEPALIVE_*`                           |
| 畸形 / 不完整帧          | 内容超 128 字符整帧丢弃（从机侧）；缺帧尾 200ms 强制结帧（主机侧）                                   | `NET_FRAME_MAX_LEN` / `PORT_RX_TIMEOUT_MS` |

## 7. 从机内部三层结构（模块化解耦）

```text
F8266-x/src/
├── net_config.h   # 全部网络/主机参数（改配置只动此文件）
├── wifi_net.*     # 联网层：STA 直连 + 配置热点兜底 + 断线自愈（封装 WiFiManager）
├── host_link.*    # 通信层：UDP 发现主机 + TCP 连接 + @…/ 帧收发 + 断线重连
├── *_sensor.*     # 业务层传感（按项目配置）：F8266-1 ×5（sht30 温湿度 / bh1750 光照 / sgp30 eCO₂+TVOC（I2C 共线）、mq2 可燃气体（ADC 分压）、soil 土壤湿度（DO GPIO14））；F8266-2 ×3（gy39 气象二合一【I2C 直连芯片直读】替代 sht30+bh1750 / soil 改 AO 模拟量经分压→A0 / sgp30）
├── actuator_ctrl.* # 业务层执行（首个）：水泵（GPIO0 高触发）/ 风扇正反转（GPIO13/2 低触发软件互锁）/ 舵机（GPIO12）
├── oled_ctrl.*    # 业务层显示：SSD1306 状态屏，标题 1 行 + 信息区 6 行（每子系统一行）
└── main.cpp       # 仅编排：初始化各模块 + loop 调各 handle + onHostCommand() 命令分发
```

约定：每个业务功能一个模块（`xxx_ctrl.h/.cpp`，只暴露少量接口）；第三方库 include 只写在 `.cpp`；主机命令的解析与执行集中在 `main.cpp` 的 `onHostCommand()`；从机主动上报用 `link.send("内容")`（自动包帧）。详见各项目 README 的「模块扩展指引」。

## 8. 扩展指南

### 8.1 新增一台从机 F8266-x

1. 复制 `F8266-1/` 为 `F8266-x/`（F8266-2 即按本流程创建，可参照；复制后按该从机定位增删传感模块——F8266-2 即删 `mq2_sensor.*`、SHT30+GY-30 合并为 GY-39、土壤改 AO 模拟量的先例，上报 `soil` 保持 0/1 两态则主机协议无需改动）；
2. 改 `src/net_config.h`：`NET_DEVICE_NAME` → `"F8266-x"`，`NET_HOST_PORT` → `8000+(x-1)`；
3. 同步改项目 README（标题/命名/更新记录）与 `platformio.ini` 环境名；
4. 在仓库根 [README](README.md) 索引表登记，并在本文档 §5 端口分配表更新状态；
5. 硬件接外设后按 §7 约定加业务模块。

### 8.2 命令词表（已定义，执行器控制）

主机网页按键 / 控制台 `/api/send` 下发的命令内容（帧自动包 `@…/`），从机 `onHostCommand()` 解析执行：

| 命令            | 动作                     | 从机执行                    |
| --------------- | ------------------------ | --------------------------- |
| `PUMP ON`       | 水泵开                   | `act.pumpOn()`              |
| `PUMP OFF`      | 水泵关                   | `act.pumpOff()`             |
| `FAN FWD`       | 风扇正转                 | `act.fanForward()`          |
| `FAN REV`       | 风扇反转                 | `act.fanReverse()`          |
| `FAN STOP`      | 风扇停                   | `act.fanStop()`             |
| `SERVO <0~180>` | 舵机转到指定角度         | `act.servoSet(deg)`         |

- 上行：从机 `link.send()` 上报传感数据 + **执行器实时状态**（JSON，见 §4.2），主机网页执行器卡据此实时回显；无独立回包；
- **阈值自动控制（主机侧 `auto_ctrl`，已实现）**：从机本地不做自动逻辑，统一由主机判定——高温(≥`AC_T_HIGH`)/高CO₂(≥`AC_CO2_HIGH`)→下发 `FAN FWD`，土壤干(`soil==0`)/低湿(`h<AC_H_LOW`)→下发 `PUMP ON`，回到安全区（带滞回）→`FAN STOP`/`PUMP OFF`；每条规则连续越界/回安全区 `AC_CONFIRM_N` 次才翻转（防抖）；总开关 + 3 阈值经网页 `/api/auto` 可改并持久化 NVS（详见 esp32-2 现行（esp32-1 备份同构）的 auto_ctrl 模块）。

### 8.3 主机侧扩展

按 esp32-2（现行）模式（esp32-1 备份同构）：新接口在 `web_ui.cpp` 注册路由；新逻辑模块建 `xxx.h/.cpp` 暴露 `begin()/loop()`；参数集中 `config.h`。

## 9. 当前进度 vs 完整拓扑

| 里程碑                                             | 状态                                        |
| -------------------------------------------------- | ------------------------------------------- |
| 主机 6 路 TCP 端口 +`@…/` 帧协议 + 上线识别     | ✅                                          |
| 主机 UDP 广播宣告 + 从机动态发现（主机可 DHCP）    | ✅                                          |
| 主机 TCP keepalive 掉电感知                        | ✅                                          |
| 主机网页控制台（状态卡 + 端口收发窗 + API）        | ✅ esp32-1（备份）；现行 esp32-2 升级为网页 v2 仪表盘 |
| 现行主机 esp32-2（每端口独立 auto_ctrl + 网页 v2） | ✅ 现行部署；esp32-1 降为旧版备份             |
| 从机 F8266-1：联网（直连/配置热点/自愈）+ 主机链路 | ✅                                          |
| F8266-1 业务层第一步：OLED 状态屏                  | ✅                                          |
| 子系统① SHT30 温湿度采集 + 读数/故障上屏（OLED 信息行） | ✅ 第一个传感实体已接入                  |
| 子系统② GY-30(BH1750) 光照采集 + 上屏（OLED 信息区多行） | ✅ 信息区扩为 4 行，每子系统一行        |
| 子系统③ MQ-2 可燃气体采集 + 上屏（ADC，首个模拟量子系统） | ✅ 独立供电+共地+分压，预热/报警/合理性模型 |
| 子系统④ 土壤湿度监测 + 上屏（DO 数字量 GPIO14）    | ✅ 高=湿润/低=过干 + 1s 环境消抖           |
| 子系统⑤ SGP30 空气质量 + 上屏（I2C 0x58，eCO₂ 等效值+TVOC） | ✅ 15s 暖机；真 CO₂ 需 SCD40（后续从机另议） |
| 首个执行实体：水泵/风扇/舵机（actuator_ctrl）      | ✅ 接口就绪（泵高触发/扇低触发+安全态+互锁+上电自检） |
| 传感数据 JSON 上报 + 主机网页图形化 + 执行器远程控制 | ✅ 从机 2s 上报（含执行器状态回传 pump/fan/servo）、主机数据卡+进度条、执行器按键/滑条、日志二级页 |
| 命令词表 / 业务协议定义                            | ✅ 已定义（PUMP/FAN/SERVO），见 §8.2 |
| 主机阈值自动控制 auto_ctrl                        | ✅ 4 规则（高温/高CO₂→风扇、土壤干/低湿→水泵）滞回+连续 N 次防抖，阈值 NVS 持久化 + 网页可配 |
| 智慧农场其余传感/执行实体接入 + OLED 汇总展示      | 🔄 传感①~⑤ + 执行器已接入，OLED 信息区 6 行满员，后续上屏需再改布局 |
| F8266-2 ~ F8266-6 从机                             | ✅ F8266-2 已上线（差异化：无 MQ-2、GY-39 直连二合一、土壤 AO→A0，2026-09-09 上板联调通过，光照已按 F8266-1 并排对照标定 ×14；土壤阈值为估算默认待标定）；F8266-3~6 端口与机制已预留 |
| esp8266-1 定位（保留演示 / 改造为从机 / 归档）     | ⬜ 待定                                     |

## 10. 维护约定

本文档只维护**全局拓扑与跨项目约定**，各板卡实现细节由各项目 README 维护：

1. 架构级变更（新增/移除节点、端口分配、帧协议格式、发现机制、命名体系）必须同步本文档对应章节 + 「更新记录」补一行 + 更新文档日期；
2. 新增板卡项目时：项目 README + 仓库根 README 索引表 + 本文档 §3/§5 三处同步登记；
3. 代码、文档放同一次 git 提交（或紧邻两次提交）推送。

## 更新记录

| 日期       | 变更内容                                                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-06 | 创建：沉淀预期完整拓扑（用户层 / 主机 / 从机层 / 独立岛）、发现面·数据面·控制面协议、自愈兜底机制、从机三层结构与扩展指南、进度基线           |
| 2026-09-06 | §2 新增图片版拓扑图`docs/topology.svg`（三通信面分色 + 实现状态标注，浅色/深色主题自适应，双主题渲染自检通过） |
| 2026-09-06 | §9 进度更新：F8266-1 子系统① SHT30 温湿度采集接入（I2C 0x44 与 OLED 共线，2s 采集 + 故障判定 + 热插拔重扫），读数/故障经 OLED 信息行上屏——首个传感实体落地 |
| 2026-09-06 | §9 进度更新：F8266-1 子系统② GY-30(BH1750) 光照采集接入（I2C 0x23 共线，同构自愈机制）；OLED 信息区扩为 4 行（每子系统一行），传感实体扩展模式成型 |
| 2026-09-07 | §9 进度更新：F8266-1 子系统③ MQ-2 可燃气体采集接入——首个模拟量（ADC）子系统：独立 5V 供电+共地+5×1kΩ/1kΩ 分压、中值滤波、预热/阈值报警/贴边合理性模型 |
| 2026-09-07 | §9 进度更新：F8266-1 子系统④ 土壤湿度监测接入——首个数字量（DO→GPIO14）子系统：高=湿润/低=过干 + 1s 环境消抖；OLED 信息区 4 行满员，后续子系统需规划整页布局（缩字号/轮播） |
| 2026-09-08 | 进度载体同步：拓扑图 F8266-1 卡片更新至传感子系统④（基线日期 2026-09-08，重渲染位图版）；§2 去除悬空句并补充位图版说明；§3/§5/§7 措辞对齐子系统①~④现状 |
| 2026-09-08 | §9 进度更新：F8266-1 子系统⑤ SGP30 空气质量接入（I2C 0x58 共线，eCO₂ 等效值+TVOC，15s 暖机）；OLED 布局改造——标题压 1 行（OLED OK 退役），信息区 4→6 行（已用 5 行）。注：eCO₂ 非 CO₂ 真值，后续从机如需真 CO₂ 选 SCD40 |
| 2026-09-08 | §9 进度更新：F8266-1 首个执行实体接入——水泵（GPIO0 高触发）/ 风扇双路正反转（GPIO13/2 低触发，软件互锁防驱动级直通短路）/ 舵机（GPIO12）；上电安全态（泵关/扇停/舵机 90°）+ 逐个激活 0.5s 上电自检，OLED 行 5 上屏（信息区满员）；控制入口待命令词表定义 |
| 2026-09-08 | §4.2/§4.3/§8.2/§9 更新：数据面新增传感 JSON 上报协议（从机 2s 发 `@{"t":..,"h":..,"lux":..,"soil":..,"co2":..,"tvoc":..}/`，不含 MQ-2，帧长 64→128）；命令词表定义（PUMP/FAN/SERVO）；主机网页改传感数据卡+进度条+执行器按键+日志二级页；命令词表与远程控制里程碑 ✅ |
| 2026-09-08 | 全架构同步至最新代码：§3/§4.2/§4.3/§5/§6/§8.2/§9 更新——传感 JSON 上报补执行器实时状态回传（pump/fan/servo）；控制面新增 `/api/auto`（auto_ctrl 阈值配置/规则状态）；新增「主机阈值自动控制 auto_ctrl」说明（4 规则滞回+防抖+NVS+网页可配）；§6 帧长 64→128 纠误；进度表补「执行器状态回传」与「auto_ctrl」两里程碑 ✅；从机层「控制入口待接」改为已接 |
| 2026-09-08 | §6 自愈表新增「主机断开/从机断网 → 从机 allStop() 全停执行器（泵关/扇停/舵机回中位），重连成功立即补发设备状态，重连后不自动恢复」安全机制 |
| 2026-09-08 | 舵机语义定为**大棚开合**（0°=关棚/180°=开棚）：§4 数据帧示例 servo 归 0、§6 allStop 回 0° 关棚；esp32-1 auto_ctrl 改为智能大棚控制（高温/高CO₂→风扇正转散热、高湿→风扇反转排湿、温/CO₂/高湿超阈 20% 开棚、土壤干→水泵），阈值 hLow→hHigh |
| 2026-09-08 | 从机扩展①：新增 F8266-2（由 F8266-1 完整克隆，§8.1 流程首次实操）——身份 `F8266-2`、端口 8001、环境名 `f8266_2`，MAC 出厂唯一无需处理；§1/§3/§5/§9 与拓扑图同步，编译通过（RAM 37.4%/Flash 36.7%），待上板联调 |
| 2026-09-08 | F8266-2 **定位差异化**（非智慧农场）：删 `mq2_sensor.*`（MQ-2 退出，A0 让出）；土壤湿度 DO 数字量→**AO 模拟量**（5×1k/1k 5:1 分压→A0，1s 5 采样中值滤波 + 滞回双阈值 300/420 判湿干 + 贴顶提醒，阈值须现场标定），OLED 信息区重排 5 行；上报 `soil` 保持 0/1 两态，主机协议与 auto_ctrl 不受影响；§1/§3/§5/§7/§8.1/§9 与拓扑图卡片同步，编译通过（RAM 37.4%/Flash 36.6%） |
| 2026-09-09 | F8266-2 传感合并：删 `sht30_sensor.*`+`bh1750_sensor.*`，新增 `gy39_sensor.*`（**GY-39 气象二合一**：BME280 温/湿/气压 + MAX44009 光照，I2C 0x5B 与 OLED/SGP30 共线，须 S0 焊桥接 GND 的 I2C 模式、CT=SCL/DR=SDA；2s 直读 0x00~0x0D 寄存器 + 物理范围合理性监测 + 健康判定/热插拔重扫）；上报 JSON 新增 `press`(hPa)，t/h/lux 字段不变；§1/§3/§4.2/§5/§7/§8.1/§9 与拓扑图卡片同步，编译通过（RAM 37.4%/Flash 36.5%） |
| 2026-09-09 | F8266-2 GY-39 采集改 **UART 免焊 / I2C 双模式自适应**（§7 从机结构同步）：UART 路径 SoftwareSerial 9600bps 只收（CT→GPIO14，出厂默认模式免焊即用）解析官方帧协议（0x15 光照帧/0x45 气象帧，累加和校验）；与 I2C 0x5B 探测并行、先到先用，帧流停 30s 降级回探测支持接线热切换；编译通过（RAM 37.7%/Flash 37.2%） |
| 2026-09-09 | F8266-2 GY-39 增补**直连芯片模式**（现场实测：模块板边 SDA/SCL 焊盘直连芯片绕过 MCU，总线扫描 0x4A+0x76 应答）：BME280@0x76/0x77（Adafruit 库）+ MAX44009@0x4A（直读寄存器）上共享 I2C 总线；至此直连/UART/I2C-MCU 三模式自适应，接线热切换自愈；§7 同步；编译通过（RAM 37.7%/Flash 37.7%） |
| 2026-09-09 | F8266-2 GY-39 收敛为**直连芯片单一路径**（删除 UART / I2C-MCU 备选路径）；光照加 ×100 现场标定倍率（原始字节实测该模块 MAX44009 读数恒偏低约百倍，固定倍率偏差）；§7 同步；编译通过（RAM 37.4%/Flash 37.0%） |
| 2026-09-09 | F8266-2 **上板联调通过**：§1/§3/§5/§9 状态与拓扑图卡片（待上板→已实现）同步为已上线；遗留现场标定项（GY-39 光照 ×100 倍率、土壤 AO 湿干阈值）已在 §9 注明 |
| 2026-09-09 | **现行主机改为 esp32-2**：§1 最终形态、§3 层级职责、§5 端口/身份/回退、§6 兜底热点名、§9 进度表同步——esp32-2（网页 v2 + 每端口独立 auto_ctrl）为现行部署，esp32-1 降为旧版备份；mDNS 现行 `esp32s3-2.local` / 热点 `ESP32-Direct-2` |
