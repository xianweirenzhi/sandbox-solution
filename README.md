# sandbox-solution

> 多板卡固件项目集合。本文档由 Claude Code 维护，随仓库变更同步更新。最后更新：2026-09-08

## 仓库结构

每块开发板 / 每个实验项目独立一个顶层目录（`<板卡>-<序号>`），互不干扰，避免上传冲突：

| 目录 | 板卡 | 说明 |
| --- | --- | --- |
| [esp8266-1/](esp8266-1/) | Adafruit HUZZAH ESP8266 | SoftAP 开放热点 + 内置 ESP8266 介绍页 + 连接自动跳转（强制门户） |
| [F8266-1/](F8266-1/) | Adafruit HUZZAH ESP8266 | esp32-1 主机的 TCP 从机：WiFi STA 联网（默认网络直连 + 同名配置热点兜底 + 断线自愈）+ 主机通信层（UDP 广播**动态发现主机 IP**、上报上线、`@命令/` 帧收发，命令内容留扩展口）；业务层智慧农场：OLED 状态屏 + 传感子系统①~⑤（温湿度/光照/可燃气体/土壤湿度/空气质量）+ 执行实体（水泵/风扇/舵机，接口就绪待接控制） |
| [esp32-1/](esp32-1/) | 4D Systems gen4-ESP32S3 (R8N16) | 主机：STA（DHCP/固定 IP，超时切 AP 兜底）+ 设备状态网页 + 6 路 TCP 从机通信端口（F8266-x 上下线/掉电管理）+ **UDP 广播宣告**供从机动态发现 |

> 新板卡项目请在仓库根新建同级目录（如 `esp32-1/`），并在此表登记。
> 全仓整体架构与预期完整拓扑见 [ARCHITECTURE.md](ARCHITECTURE.md)。

## 开发环境

- Windows 11 + VSCode（PlatformIO IDE 扩展）
- PlatformIO Core（`~/.platformio/penv/Scripts/pio.exe`）

```bash
pio run -d esp8266-1              # 构建指定项目
pio run -d esp8266-1 -t upload    # 烧录指定项目
pio run -d F8266-1                # 构建 F8266-1（HUZZAH WiFi STA 联网从机项目）
```

## 更新记录

| 日期 | 变更内容 |
| --- | --- |
| 2026-09-01 | 建立 Git 仓库并推送至 GitHub，初始为单项目结构 |
| 2026-09-04 | 仓库改造为多板卡项目集合结构：esp8266 项目迁入 `esp8266-1/`，新增仓库级 README |
| 2026-09-04 | 新增 `esp32-1/`：ESP32-S3 STA/AP 双模式设备状态服务（STA 优先 + 超时 AP 兜底 + 状态网页），新位置编译通过 |
| 2026-09-04 | `esp32-1/` 升级为主机控制台：STA 固定 IP、新增 6 路 TCP 从机通信端口（F8266-x 上下线识别）、网页 2×3 端口收发窗口 + 精简状态卡 |
| 2026-09-04 | 新增 `F8266-1/`：ESP8266(ESP-01) WiFi STA 联网模块化项目——默认网络直连（ESP-TEST）+ 同名配置热点 F8266-1 兜底 + 断线自愈，代码分模块并附 README，编译通过 |
| 2026-09-05 | `F8266-1/` 接入主机通信层：新增 `host_link` 模块，联网后作为 TCP 客户端连主机 `192.168.1.100:8000`，连上上报 `@F8266-1 online/`，`@…/` 帧收发 + 断线自动重连；命令内容留 `onHostCommand()` 扩展口，编译通过 |
| 2026-09-05 | `F8266-1/` 构建板型由 ESP-01 换为 Adafruit HUZZAH ESP8266（`board=huzzah`，4MB Flash），说明文档同步 |
| 2026-09-05 | esp32-1 新增 `host_announce`：UDP 广播宣告自身 IP/端口（供从机动态发现）；F8266-1 增加 UDP 广播**动态发现主机**（超时回退硬编码地址）。从机不再依赖主机固定 IP，主机可保持 DHCP |
| 2026-09-05 | `F8266-1/` 业务层智慧农场起步：新增 `oled_ctrl` 模块激活 0.96" SSD1306 OLED（状态可视化层入口，首屏设备名/项目名/OLED OK），依赖 Adafruit SSD1306/GFX，编译通过 |
| 2026-09-06 | 新增仓库级架构文档 [ARCHITECTURE.md](ARCHITECTURE.md)：沉淀预期完整拓扑（用户层 / esp32-1 主机 / F8266-x 从机层 / esp8266-1 独立岛）、发现面·数据面·控制面协议、自愈兜底机制、扩展指南与进度基线；附图片版拓扑 `docs/topology.svg`（浅色/深色自适应） |
| 2026-09-06 | `F8266-1/` 子系统①接入：新增 `sht30_sensor` 模块（SHT30 温湿度，I2C 0x44 与 OLED 共线，2s 采集 + 故障判定 + 热插拔重扫），`oled_ctrl` 增 `setInfoLine` 信息行接口，读数/故障实时上屏，编译通过 |
| 2026-09-06 | `F8266-1/` 子系统②接入：新增 `bh1750_sensor` 模块（GY-30/BH1750 光照，I2C 0x23 共线，同构子系统①自愈机制），`oled_ctrl` 信息行升级为信息区 4 行（`setInfoLine(row, ...)` 每子系统一行），编译通过 |
| 2026-09-07 | `F8266-1/` 子系统③接入：新增 `mq2_sensor` 模块（MQ-2 可燃气体，ADC A0 模拟量：独立 5V 供电+共地+5×1kΩ/1kΩ 分压、5 采样中值滤波、3 分钟预热管理、阈值报警、贴边合理性监测），OLED 行 2 上屏，编译通过 |
| 2026-09-07 | `F8266-1/` 子系统④接入：新增 `soil_sensor` 模块（土壤湿度 DO→GPIO14 数字量，3V3 供电，高=湿润/低=过干 + 1s 环境消抖），OLED 行 3 上屏，信息区 4 行满员，编译通过 |
| 2026-09-08 | 进度载体同步：拓扑图（`docs/topology.svg`/`.png`）F8266-1 卡片与 [ARCHITECTURE.md](ARCHITECTURE.md) §2/§3/§5/§7 更新至传感子系统④现状，两份文档头部日期对齐 |
| 2026-09-08 | `F8266-1/` 子系统⑤接入：新增 `sgp30_sensor` 模块（SGP30 空气质量，I2C 0x58 共线，eCO₂ 等效值+TVOC，15s 暖机），OLED 布局改造（标题压 1 行，信息区 4→6 行），编译通过 |
| 2026-09-08 | `F8266-1/` 执行器极性调整 + 上电自检：水泵改高触发（GPIO0 烧录拉低=关，消除烧录误开）、风扇保持低触发；新增 `selfTest` 逐个激活执行器 0.5s 上电自检，编译通过 |
| 2026-09-08 | `F8266-1/` 执行实体接入：新增 `actuator_ctrl` 模块（水泵 GPIO0 / 风扇正反转 GPIO13·2 / 舵机 GPIO12），上电安全态 + 风扇软件互锁，OLED 行 5 上屏，控制入口待接，编译通过 |
| 2026-09-08 | 主从业务闭环打通：从机 2s 传感 JSON 上报（不含 MQ-2）+ 命令词表（PUMP/FAN/SERVO）；主机端 ArduinoJson 解析、网页改「传感数据卡+进度条+执行器按键+日志二级页」；帧长 64→128；两端编译通过 |
| 2026-09-08 | `esp32-1/` 修复 `parseData` 首条日志永不打印的缺陷（`hasData` 置位先于判断致条件恒假）；调整为判断后再置位，收到的首条传感帧会正常记录「⚙ 开始接收传感数据」 |
